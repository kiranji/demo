pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'docker build -t selenium-tests . && docker run selenium-tests' // Build and run tests in container
            }
        }
    }
}
