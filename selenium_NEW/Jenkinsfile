pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'docker build -t selenium-tests selenium_NEW/. && docker run selenium-tests' // Build and run tests in container
            }
        }
    }
}
